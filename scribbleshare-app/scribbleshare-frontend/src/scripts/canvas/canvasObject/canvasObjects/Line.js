import LineCanvasObject from "../LineCanvasObject.js";

export default class Line extends LineCanvasObject {
    constructor(reader) {
        super(reader);
    }

    serialize(writer) {
        super.serialize(writer);
    }
}